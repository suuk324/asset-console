# FluxMint 设计资产台宣发视频策划（45 秒逐秒脚本）

## 1. 这条视频怎么拍

- 视频类型：`9:16` 竖屏短视频
- 建议时长：`45 秒`
- 目标受众：设计师、工作室、小团队、需要在电脑和手机之间看项目文件的人
- 核心卖点：把真实项目文件夹集中到一个工作台里，统一预览、整理、导入和调用；同一 Wi-Fi 下，手机还能直接访问工作目录
- 建议发布场景：抖音、视频号、小红书、B 站竖屏切条

## 2. 这条视频的主标题

`设计文件别再满电脑乱飞了，FluxMint 让电脑和手机都能直接接上你的真实项目目录。`

## 3. 你拍之前必须补齐的东西

### 3.1 演示项目先准备好

建议你提前准备一个干净的演示目录，名字尽量简单，比如 `专题设计1`，目录结构建议如下：

```text
专题设计1/
  参考图/
    moodboard_01.jpg
    cmf_reference.png
  3D模型/
    chair_model_v12.3dm
  Final输出/
    render_final_A.png
    汇报方案.pdf
  文档/
    交付说明.txt
```

### 3.2 软件里建议提前准备好的状态

- `项目总览`里已经绑定好 `专题设计1`
- `规则`页里至少有 1 条规则，建议用“最终输出模板”或“3D 文件模板”
- `设置 -> 手机端局域网服务`里已经选好工作目录
- 手机和电脑处于同一 Wi-Fi
- 手机浏览器能正常打开电脑端显示的局域网地址
- 文件里至少有一张图片、一个 PDF、一个文本文件，方便演示预览

### 3.3 拍摄素材建议

- 桌面端录屏一份：`1920x1080`
- 手机端录屏一份：竖屏
- 可选实拍镜头一份：手机拿在手里输入连接码
- BGM 建议：`110-125 BPM`，偏科技、效率、工作流感
- 字幕建议：每句不超过 `14` 个汉字

## 4. 45 秒逐秒脚本

说明：

- `屏幕文案` 是最终字幕文案
- `软件演示动作` 是你这一秒要做的操作
- `剪辑提示` 是后期怎么卡节奏
- 真实录制时，每个动作先慢慢录，后期再压到对应秒数，不要试图一次实时演完整条

| 秒数 | 屏幕文案 | 软件演示动作 | 剪辑提示 |
| --- | --- | --- | --- |
| 0 | 设计文件一多，最怕越整理越乱 | 打开 `FluxMint 设计资产台`，停在 `项目总览` 首页 | 从黑场直接切进软件界面 |
| 1 | 先看项目，再看文件 | 镜头停在 `项目总览` 标题和总览卡片 | 轻微放大 `103%` |
| 2 | 最近项目、最近文件、最近操作，一屏看全 | 轻微向下滚动，让 `最近项目` 和 `最近文件` 出现在同一画面 | 用平滑滚动，不要太快 |
| 3 | 点进项目，立刻进入工作区 | 点击 `专题设计1` 进入项目页 | 配一个清脆点击音效 |
| 4 | 左边目录，右边文件，结构马上清楚 | 停在项目页，让目录树和文件区完整出现 | 这一秒不要乱动，让观众看布局 |
| 5 | 真实目录直接映射，不是空壳演示 | 点击左侧 `参考图` 文件夹 | 字幕压在中上位置 |
| 6 | 文件选中就能看重点内容 | 点击一张图片文件 | 让预览区跟着变化 |
| 7 | 图片、文档、项目资料集中查看 | 停留 0.8 秒展示预览和文件信息 | 可做一次局部放大 |
| 8 | 顶部直接搜，不用一层层翻 | 点击顶部搜索框 | 镜头跟着搜索框移动 |
| 9 | 输入关键词，结果马上出来 | 输入 `render` 或 `final` | 建议后期用键盘打字音 |
| 10 | 找文件，从分钟级变成秒级 | 展示搜索结果命中的文件 | 让结果高亮停住半秒 |
| 11 | 导入也能收进当前项目里 | 点击 `导入到当前项目`，选入 1 个演示文件 | 这段可提前单独录，再拼接 |
| 12 | 文件进来后，继续统一管理 | 回到项目文件区，看到新文件已经出现 | 用一个快速闪白转场 |
| 13 | 不只是收纳，还能提前定规则 | 点击左侧 `规则` | 转到规则页时加轻推镜头 |
| 14 | 常见素材类型，直接套模板 | 点击一个规则模板，比如 `最终输出模板` | 模板按钮要录到明显按下反馈 |
| 15 | 关键词、格式、目标目录，一次定好 | 让表单里的 `关键词 / 格式 / 目标目录` 出现在同一屏 | 重点框一下目标目录 |
| 16 | 后面再导入，落点更省脑子 | 点击 `保存` | 给一个确认感的音效 |
| 17 | 电脑端整理完，手机也能接上 | 点击左侧 `设置` | 节奏开始往“手机联动”过渡 |
| 18 | 这里直接开手机端局域网服务 | 停在 `手机端局域网服务` 模块 | 这一秒保持稳，给观众读信息 |
| 19 | 先选工作目录 | 点击 `选择工作目录` | 如果选择过程太长，提前录好并裁切 |
| 20 | 再一键启动服务 | 点击 `启动服务` | 按钮点击瞬间切一帧音效 |
| 21 | 地址出来了 | 聚焦 `访问地址` 区域 | 放大地址卡片 |
| 22 | 连接码也出来了 | 聚焦 `连接码` 卡片 | 可给连接码加描边强调 |
| 23 | 谁连上来，电脑端也看得到 | 聚焦 `当前连接设备` 区域，先展示空状态也可以 | 为后面手机上线做铺垫 |
| 24 | 手机打开地址，直接进面板 | 切到手机录屏，打开桌面显示的局域网地址 | 建议这里做分屏或手机壳包裹 |
| 25 | 输入连接码 | 在手机登录页输入 6 位连接码 | 用打字节奏卡音乐点 |
| 26 | 同一 Wi-Fi，下发即连 | 点击 `进入文件面板` | 登录成功后直接切到列表页 |
| 27 | 工作目录内容，手机立刻能看 | 展示手机端根目录列表 | 让目录列表轻微滑动一下 |
| 28 | 文件夹能进，路径很清楚 | 点击一个文件夹进入下一层 | 路径条要录到变化 |
| 29 | 搜索照样能用 | 点击手机端 `搜索文件名` 输入框 | 镜头跟着顶部工具栏 |
| 30 | 找图、找文档，不用回电脑 | 输入关键词并展示匹配结果 | 结果出现时稍停半秒 |
| 31 | 点开文件就能看详情 | 点击一个图片或 PDF 文件 | 用顺滑页面切换 |
| 32 | 能预览的，手机直接预览 | 展示文件详情页的预览区 | 图片或 PDF 最适合这秒 |
| 33 | 下载前，先确认文件信息 | 让 `文件大小 / 修改时间 / 预览能力` 出现 | 不要滑太快，给信息留读秒 |
| 34 | 临时传个文件，也能从手机直接传 | 返回列表页，点击 `上传文件` | 这段最好提前准备好要传的文件 |
| 35 | 上传过程有反馈，不会盲传 | 展示上传进度条 | 进度条最好录到 30%-100% |
| 36 | 上传完，列表自动刷新 | 让新文件出现在列表里 | 可以用一个高亮框新文件 |
| 37 | 文件名不对？手机上直接改 | 打开某个文件详情，点击 `重命名` | 从列表到详情可以提前剪好 |
| 38 | 改完立刻生效 | 输入新名称并确认 `确认重命名` | 字幕和动作都要干脆 |
| 39 | 要原文件，直接下载 | 点击 `下载原文件` | 配一个“已开始下载”提示截图 |
| 40 | 手机一连上，电脑端同步看见设备 | 切回桌面端 `当前连接设备` 区域，展示手机在线 | 用前后对照增强联动感 |
| 41 | 电脑整理，手机访问，流程打通了 | 同屏展示桌面端设置页和手机端文件页 | 这一秒建议做分屏 |
| 42 | 不改你的真实项目逻辑 | 切回桌面项目页，扫过真实目录和文件结构 | 强调“真实目录映射” |
| 43 | 只是把管理、预览和访问都集中起来 | 快速闪过 `项目页 -> 规则页 -> 手机页` 三连切 | 3 个镜头各 0.3 秒左右 |
| 44 | FluxMint 设计资产台 | Logo 或软件标题停住，底部放一句 `把真实项目文件夹变成可管理工作台` | 结尾定格 `0.8-1 秒` |

## 5. 这条视频最重要的拍法

- 不要一上来讲“功能很多”，先讲“文件终于不乱了”
- 桌面端展示重点不是炫 UI，而是让观众看懂 `真实项目目录映射 + 搜索 + 规则 + 导入`
- 手机端展示重点不是完整操作教学，而是证明 `同一 Wi-Fi 下真的能连、能找、能看、能传、能改`
- 每个镜头只讲一个点，不要一秒里塞两层信息

## 6. 我帮你补齐的拍摄执行建议

### 6.1 最适合这版视频的结构

- 前 `0-10` 秒：让人明白这是“整理真实项目文件”的软件
- 中间 `11-23` 秒：证明它不只是看文件，还能导入、设规则、开局域网服务
- 后 `24-40` 秒：证明手机端真的能用
- 最后 `41-44` 秒：收口价值和品牌名

### 6.2 你录屏时要注意

- 鼠标移动要慢，后期再加速
- 点击前先停 `0.2-0.4` 秒，方便剪辑卡点
- 手机录屏尽量关通知
- 桌面和手机都用同一套演示文件，避免前后对不上
- 如果录制时局域网地址太长，后期可以局部放大，不要全屏硬缩

### 6.3 如果你想让片子更像“成品广告”

- 开头加一句口播：`设计文件管理，不该靠记忆力。`
- 中段加一句口播：`电脑整理，手机直接接着看。`
- 结尾加一句口播：`FluxMint，把真实项目文件夹变成工作台。`

## 7. 这条视频最容易翻车的点

- 演示目录太乱，观众看不懂结构
- 项目名太长，字幕和画面会互相打架
- 手机端没有提前连好 Wi-Fi，现场容易卡壳
- 连接码、地址、设备列表没有录到关键状态变化
- 想把所有功能都塞进 45 秒，结果每个点都没讲清

## 8. 建议你直接照着拍的录制顺序

1. 先录桌面端 `项目总览 -> 项目页 -> 搜索 -> 导入 -> 规则 -> 设置`
2. 再录桌面端 `局域网服务启动后` 的地址、连接码、设备列表
3. 最后录手机端 `登录 -> 浏览 -> 搜索 -> 预览 -> 上传 -> 重命名 -> 下载`
4. 全部录完再按上面的逐秒表压节奏剪

如果你下一步需要，我可以继续直接帮你补：

- `30 秒精简版脚本`
- `口播版逐句台词`
- `字幕文件 SRT`
- `封面标题和发布文案`
