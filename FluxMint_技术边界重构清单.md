# FluxMint 技术边界重构清单

## 1. 结论先行

FluxMint 现在遇到的很多“用起来不像原生桌面软件”的问题，根因不是单纯的 UI，而是**技术边界还没有切干净**。

更准确地说：

- `React/WebView` 适合做界面、状态表达、交互编排。
- `Tauri/Rust/Windows API` 适合做真实文件、系统拖拽、系统预览、文件监控、外部软件调用。
- 目前的问题，主要来自**把本该原生负责的行为继续放在了浏览器语义里处理**。

因此，FluxMint 后续不应该被继续当成“网页套壳”，而应该明确重构为：

`原生文件引擎 + Web 工作台`

---

## 2. 当前问题的本质

### 2.1 典型现象

- 文件拖到桌面时触发“下载失败 / 网络问题”
- 浏览器内预览没问题，但桌面外部软件不一定能接住拖拽
- 文件移动、重命名、解绑、删除之后，界面和真实磁盘容易出现短暂不一致
- 有些交互在浏览器预览里成立，但在真正桌面运行时不成立

### 2.2 根因

这些问题本质上都指向同一个判断：

**当前有一部分系统级行为，仍然被当成 Web 行为在处理。**

例如：

- “拖文件到桌面”不能用浏览器的 `DownloadURL` 语义冒充
- “拖到 Rhino / Blender / KeyShot”不能只依赖 HTML5 `DataTransfer`
- “文件是否存在 / 是否改名成功 / 是否真的移动到目标目录”不能只靠前端状态推断
- “系统缩略图 / 原生格式封面”不能完全依赖浏览器预览链路

---

## 3. 目标边界

FluxMint 后续的目标边界应固定为三层：

### 3.1 Web 工作台层

职责：

- 页面布局
- 目录树展示
- 文件网格 / 列表展示
- Inspector 展示
- 搜索输入、筛选输入、排序输入
- 导入面板、规则面板、设置面板
- 拖拽反馈动画、空状态、错误提示、主题与语言切换

技术位置：

- `src/pages/*`
- `src/components/*`
- `src/styles/*`
- `src/i18n/*`

这一层**不应该**承担：

- 文件是否真正移动成功的最终判断
- 桌面原生拖出
- 回收站语义
- 文件监控源头
- 系统缩略图提取

### 3.2 桥接适配层

职责：

- 把前端动作翻译成明确的桌面命令
- 隔离 “浏览器运行” 和 “桌面运行” 的行为差异
- 提供稳定的前端 API，而不是让页面直接碰 Tauri 细节

技术位置：

- `src/adapters/desktopBridge.ts`
- `src/utils/assetTransfer.ts`

这一层应成为唯一合法入口：

- 打开文件
- 显示位置
- 导入文件
- 原生拖出
- 读取原生预览
- 监听文件变化
- 回收站恢复 / 清空

### 3.3 原生文件引擎层

职责：

- 项目索引
- 文件导入
- 文件移动
- 文件删除
- 文件重命名
- 文件夹扫描
- 项目解绑
- 操作历史 / 撤销
- 回收站
- 重复文件检测
- 原生预览提取
- 文件监控
- 原生拖出

技术位置：

- `src-tauri/src/library.rs`

这一层应成为真实单一事实源：

- 前端显示什么，不应自己猜
- 前端只消费原生层返回的最新结果

---

## 4. 哪些能力必须原生化

以下能力必须由原生层负责，不能再停留在浏览器语义：

### 4.1 文件拖入 / 拖出

必须原生化原因：

- 拖到桌面、资源管理器、Rhino、Blender、Figma、KeyShot 都是操作系统级拖放
- HTML5 拖拽只适合浏览器目标，不适合完整桌面文件语义

要求：

- 拖入：通过 Tauri / 原生事件拿到真实文件路径
- 拖出：通过 Windows 原生拖拽带出真实文件，而不是下载链接

### 4.2 真实文件变更

包括：

- 导入
- 移动
- 重命名
- 删除
- 回收站恢复

必须原生化原因：

- 这类操作必须对磁盘负责
- 失败时要可回滚
- 成功后要马上刷新索引

### 4.3 文件监控与自动刷新

必须原生化原因：

- 浏览器不知道资源管理器、Rhino、Blender、KeyShot 在外部改了什么
- 只有原生 watcher 才能追踪真实目录变化

### 4.4 系统预览与特殊格式封面

包括：

- `.bip`
- `.ksp`
- 其他可通过 Windows Shell 拿到缩略图的格式

必须原生化原因：

- 浏览器本身无法稳定解析这些专有格式
- 原生缩略图提取比前端猜测更可靠

### 4.5 回收站 / 安全删除

必须原生化原因：

- 删除不是单纯从 UI 消失
- 必须有真实落点、恢复逻辑和失败处理

### 4.6 操作历史 / 撤销

必须原生化原因：

- 撤销的对象不是界面状态，而是磁盘操作
- 只有原生层知道每一步真实改了哪些路径

---

## 5. 哪些能力适合继续保留在前端

以下内容继续放在 Web 层是合理的：

### 5.1 工作台 UI

- 顶栏
- 左侧导航
- 目录树可视化
- 文件区网格 / 列表
- Inspector
- 右键菜单视觉
- 主题 / 语言

### 5.2 展示型预览

适合前端负责：

- 图片
- PDF
- 视频
- 3DM 的交互式展示层

前提：

- 原生层先把文件路径或预览资源稳定交给前端

### 5.3 交互编排

- 多选
- 筛选
- 排序
- 搜索框输入
- 导入面板流程
- 冲突处理弹层
- 成功 / 失败反馈

前端负责“流程体验”，原生负责“真实结果”。

---

## 6. 建议固定的模块边界

### 6.1 前端页面层不能直接做的事

以下做法应禁止继续扩散：

- 页面里直接拼接文件系统路径语义
- 页面里直接推断“文件应该已经移动成功”
- 页面里直接把浏览器拖拽语义当桌面拖拽语义
- 页面里直接把 `convertFileSrc()` 当作通用文件交互方案

### 6.2 前端只能通过桥接层访问桌面能力

要求：

- 所有桌面能力统一走 `src/adapters/desktopBridge.ts`
- 所有拖拽辅助统一收口到 `src/utils/assetTransfer.ts`
- 页面组件只表达“我要做什么”，不表达“操作系统该怎么做”

### 6.3 原生层返回值必须“可直接驱动 UI”

每次文件变更后，原生层应直接返回：

- 新的项目列表
- 新的目录树
- 新的资产列表
- 新的历史记录
- 必要时新的回收站数据

不要让前端用“局部 patch + 猜测”去拼结果。

---

## 7. 必须停止的反模式

以下反模式应视为后续开发禁区：

### 7.1 用 Web 下载语义模拟桌面文件拖出

典型例子：

- `DownloadURL`
- 依赖浏览器 `File` 对象希望桌面接收

风险：

- 桌面接不住
- 外部软件接不住
- 会出现“下载失败 / 网络问题”一类误导性反馈

### 7.2 把“前端显示成功”当成“文件系统操作成功”

风险：

- UI 和磁盘不一致
- 一旦刷新，状态露馅

### 7.3 浏览器预览成功就认为桌面工作流成立

风险：

- 浏览器里能看，不代表 Rhino / Blender / KeyShot 能拖入
- 浏览器里能点，不代表资源管理器会认

### 7.4 页面组件直接依赖底层平台细节

风险：

- 页面越来越重
- 调试路径分裂
- 后面想切换实现会很痛苦

---

## 8. 下一阶段最值得做的重构

按优先级拆，建议是：

### P0. 把“文件系统正确性”彻底收口到原生层

目标：

- 项目之间绝对隔离
- 子文件夹只显示自己的内容
- 绑定 / 解绑 / 重命名 / 删除 / 移动之后，索引和界面实时一致
- 文件拖动移动功能稳定

落点：

- `src-tauri/src/library.rs`
- `src/store/useAssetConsoleStore.ts`
- `src/adapters/desktopBridge.ts`

### P1. 把“外部调用”彻底原生化

目标：

- 从 FluxMint 直接拖到桌面
- 直接拖到 Rhino / Blender / KeyShot 等外部软件
- Inspector 和文件区的拖出语义一致

落点：

- `src-tauri/src/library.rs`
- `src/components/preview/AssetDetailDrawer.tsx`
- `src/pages/ProjectWorkspacePage.tsx`
- `src/utils/assetTransfer.ts`

### P1. 把“文件监控 + 索引刷新”做成默认可信能力

目标：

- 外部改名、外部删除、外部新增后能自动刷新
- 不依赖用户手动点“重新扫描”才能回正

落点：

- `src-tauri/src/library.rs`
- watcher 相关状态同步逻辑

### P2. 把“预览链路”分成原生提取和前端展示两段

目标：

- 图片 / PDF / 视频 / 3DM / BIP / KSP 各自走清晰路径
- 特殊格式优先走原生缩略图提取
- 前端只负责展示与交互

### P2. 把“错误反馈”改成系统级问题可解释

目标：

- 不再出现“网络问题”这种实际与用户操作无关的提示
- 出错时明确告诉用户是：
  - 源文件不存在
  - 目标路径冲突
  - 权限不足
  - 外部软件未接收
  - 原生拖拽启动失败

---

## 9. 建议保留的技术路线

当前阶段，不建议推翻为完全原生 UI。

更合理的路线是继续使用：

- `React + TypeScript` 负责工作台
- `Tauri 2 + Rust` 负责文件与系统能力

理由：

- UI 迭代速度更快
- 视觉完成度更容易拉高
- 当前代码资产可继续复用
- 真正的问题不在“是否用了 Web”，而在“原生边界有没有切清”

也就是说：

**不需要换框架，先把职责切对。**

---

## 10. 最终验收标准

当以下条件成立时，才算边界重构基本完成：

### 10.1 系统级行为可信

- 文件拖到桌面稳定成功
- 文件拖到外部设计软件稳定成功
- 文件移动、重命名、删除后磁盘和 UI 一致

### 10.2 前端不再伪造系统语义

- 页面不再自己“猜结果”
- 所有关键文件操作都以原生返回为准

### 10.3 桌面与浏览器预览职责清楚

- 浏览器预览只用于 UI 验证
- 真正桌面能力只在桌面运行时验收

### 10.4 出错提示可解释

- 不再出现明显属于浏览器语义的误导性错误
- 用户能判断问题出在文件、路径、权限还是目标软件

---

## 11. 一句话判断

FluxMint 后续最重要的，不是继续叠功能，而是把这条边界固定下来：

**Web 负责“看起来和用起来专业”，原生负责“文件和系统行为绝对可信”。**

只要这条边界切稳，FluxMint 才有机会真正比 Windows 资源管理器更适合设计资产工作流。
